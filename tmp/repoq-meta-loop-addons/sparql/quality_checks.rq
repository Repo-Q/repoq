# Check for basic layering violations (toy)
PREFIX code: <https://field33.com/ontologies/code/>

SELECT ?a ?b
WHERE {
  ?a a code:Module .
  ?b a code:Module .
  ?a code:imports ?b .
  FILTER(STRSTARTS(STR(?b), STR(?a))) # naive: importing deeper submodule
}
